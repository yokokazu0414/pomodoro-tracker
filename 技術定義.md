# 技術定義（ポモドーロ・タイマー）— 実装版（Firebase）

## 1. 技術スタック（採用）

| 層 | 採用 |
|----|------|
| クライアント | **Vite 6** + **React 19** + TypeScript |
| 認証 | **Firebase Authentication**（Google サインイン、`browserLocalPersistence`） |
| 永続化（正） | **Cloud Firestore** — コレクション `users/{userId}/sessions`、ドキュメント ID = `session_id` |
| チャート | **Recharts**（棒グラフ） |
| CSV | **Papa Parse**（エクスポート列固定・インポートはバッチ分割） |
| 秘密情報 | Firebase Web 設定は **環境変数（`VITE_*`）** のみ。リポジトリには **`.env.example`** のみコミットし、`.env.local` は除外 |

当初ドキュメントでは **Next.js + IndexedDB** を想定していたが、**マルチデバイス同期**のため **Firebase に変更**した。

## 2. データの正と CSV の役割

| 役割 | 内容 |
|------|------|
| 正（source of truth） | **Firestore** の `users/{uid}/sessions/{sessionId}` |
| CSV | **エクスポート**でバックアップ・手動編集。**インポート**で復元・マージ。同一 `session_id` は **merge 上書き**。CSV 内重複は **最後の行を採用** |

## 3. ドキュメントスキーマ（Firestore / CSV 共通）

| フィールド | 型・形式 | 必須 | 説明 |
|------------|-----------|------|------|
| `session_id` | 文字列（UUID） | 必須 | ドキュメント ID と一致 |
| `userId` | 文字列 | 必須 | Firebase Auth の `uid`（パスと一致） |
| `date` | `YYYY-MM-DD` | 必須 | ローカル日付 |
| `started_at` | ISO 8601 | 必須 | 開始日時 |
| `ended_at` | ISO 8601 | 必須 | 終了日時 |
| `planned_minutes` | 数値 | 必須 | 正の整数 |
| `task` | 文字列 | 必須 | **1〜200 文字**（UI・ルールで制限） |
| `reflection` | 文字列 | 必須 | **1〜200 文字**、改行なし |
| `rank` | `A` \| `B` \| `C` | 必須 | 要求定義の意味に準拠 |

- **インポート**: 同一 `session_id` が既存ドキュメントと重なる場合 **上書き**（`merge: true`）。Firestore の **1バッチ 500 操作**上限のため、インポートは **500 件ずつ** `commit`。
- **セキュリティ**: `firestore.rules` でフィールド検証・本人のみアクセス。

## 4. タイムゾーン

- **端末ローカル**で日付表示・`date` フィールドの算出・ウィークリー境界（**月曜始まり**）を行う。

## 5. タイマー状態（クライアントのみ）

- 進行中の残り時間などは **`localStorage`**（`TimerContext`）で保持し、リロード後も概ね復元。
- **同時に動くタイマーは 1 つ**（単一タブ内の状態機械）。

## 6. ダッシュボード

- Firestore の `onSnapshot` でセッション一覧を購読し、日／週／月集計と棒グラフ・履歴テーブルを表示。

## 7. セキュリティ・プライバシー

- セッション本文は **利用者の Firebase プロジェクト**に保存。任意の第三者アナリティクスへの送信は行わない（Firebase 利用はインフラとして明示）。
- ルールファイルをデプロイし、**他ユーザーの `sessions` を読めない**こと。

## 8. 参照

- `要件定義.md`
- `要求定義.md`
- `README.md`
