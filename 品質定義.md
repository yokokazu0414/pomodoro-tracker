# 品質定義（ポモドーロ・タイマー）

## 1. 受け入れ基準（全体）

| 観点 | 基準 |
|------|------|
| タイマー精度 | 実用上問題ない更新（バックグラウンドタブではブラウザ仕様により誤差が出うる） |
| データ | 保存成功時、Firestore に意図したフィールドでドキュメントが存在する。CSV 再インポートで **同一 session_id が上書き**される |
| タスク | **1〜200 文字**、未入力では開始不可 |
| 振り返り | **1〜200 文字**、**1文字以上**でないと保存不可、改行なし |
| ランク | A/B/C のみ |
| ルール | `firestore.rules` がデプロイされ、他ユーザーのデータにアクセスできない |

## 2. 手動テスト観点（最低限）

### タイマー・入力

- タスク 0 文字・201 文字で開始／保存が期待どおり制限されること。
- 振り返り空・201 文字で保存できないこと。

### データ

- セッション完了後、Firestore に 1 ドキュメント追加されること。
- CSV エクスポート → 値を変えて再インポート → **上書き**されること。
- **500 件超**の CSV でもインポートが完了すること（バッチ分割）。

### ダッシュボード

- 当日件数・グラフ・履歴がデータと矛盾しないこと。

## 3. 自動テスト（推奨）

| 対象 | 内容 |
|------|------|
| CSV 変換 | 列順・エスケープ・必須列 |
| 集計 | 週境界（月曜始まり）の単体テスト |

## 4. 参照

- `要件定義.md`
- `工程定義.md`
