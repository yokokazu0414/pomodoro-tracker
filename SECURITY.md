# セキュリティ

## 露出した API キーについて（2026-04 対応）

`firebase-applet-config.json` に **Google API Key** がリポジトリに含まれていた事案に対し、次を実施すること。

### 1. キーのローテーション（必須）

1. [Google Cloud Console](https://console.cloud.google.com/) → 対象プロジェクト → **API とサービス** → **認証情報**
2. 該当の **API キー**を開き、**キーを再生成**するか、新しいキーを作成して Firebase Web アプリの設定を更新する
3. 旧キーは **削除**または**制限**する

Firebase コンソール側では、**プロジェクトの設定** → マイアプリの Web 設定に表示されるキーが更新される場合がある。表示値に合わせて `.env.local` を更新する。

### 2. キーの制限（推奨）

- **アプリケーションの制限**: HTTP リファラー（本番ドメイン・`localhost` のみ）など
- **API の制限**: 使用する API のみに限定

### 3. Git 履歴

過去コミットに秘密が残る。**GitHub のアラートは解消後も履歴上のリスク**は残るため、上記ローテーションが実質的な対策となる。履歴から完全削除する場合は `git filter-repo` 等（上級者向け）を検討する。

### 4. ローカル設定

秘密は **`.env.local`** のみに置き、リポジトリにコミットしない。`.env.example` は変数名の例のみとする。
