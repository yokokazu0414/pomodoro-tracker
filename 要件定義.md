# 要件定義（ポモドーロ・タイマー）

## 1. 用語

| 用語 | 定義 |
|------|------|
| セッション | 1回のポモドーロ（カウントダウン1サイクル） |
| 振り返り | セッション終了時に入力する一言とランク |
| ランク | **A**／**B**／**C**。UI ラベル：A＝集中し達成できた、B＝まあまあ、C＝厳しかった・要改善 |

## 2. 機能要件

### FR-1 タイマー基本動作

| ID | 要件 | 受け入れ基準 |
|----|------|----------------|
| FR-1.1 | カウントダウンで残り時間を表示する | 1秒単位で更新され、目視で追える |
| FR-1.2 | デフォルトの作業時間は25分とする | 初回またはリセット後、25分が表示される |
| FR-1.3 | 作業時間（分）はユーザーが変更できる | 開始前に設定した値でカウントダウンが開始される |
| FR-1.4 | 同時に動作するタイマーは1つのみ | 単一クライアント状態で二重開始しない |

### FR-2 入力・振り返り

| ID | 要件 | 受け入れ基準 |
|----|------|----------------|
| FR-2.1 | 開始前にタスクを入力できる | **1文字以上・200文字以下**。未入力では開始不可 |
| FR-2.2 | 終了時に一言振り返りを入力できる | **1文字以上・200文字以下**、改行不可 |
| FR-2.3 | A/B/C のランクを選べる | 3値から選択 |

### FR-3 データ蓄積（Firestore ＋ CSV）

| ID | 要件 | 受け入れ基準 |
|----|------|----------------|
| FR-3.1 | セッションを **Firestore** に保存する | ログイン済みユーザー配下にドキュメントが作成される |
| FR-3.2 | **CSV エクスポート** | UTF-8、固定列順 |
| FR-3.3 | **CSV インポート** | 同一 `session_id` は **上書き**。大量行は **500件バッチ**。CSV 内の重複 ID は **最後の行を採用** |
| FR-3.4 | 日付・開始・終了が追える | ダッシュボード・CSV で確認できる |

### FR-4 ダッシュボード

| ID | 要件 | 受け入れ基準 |
|----|------|----------------|
| FR-4.1 | 1日あたりの実行回数を可視化する | 当日件数などが表示される |
| FR-4.2 | ランクの履歴を確認できる | 一覧／テーブルで追える |
| FR-4.3 | デイリー／ウィークリー／マンスリー | タブ切替で集計が変わる |
| FR-4.4 | 推移グラフ | 棒グラフ等で件数推移を表示 |

## 3. 非機能要件

| ID | 区分 | 要件 |
|----|------|------|
| NFR-1 | データ | Firestore を正とし、CSV でバックアップ・復元。インポート時の重複は **上書き**（技術定義） |
| NFR-2 | 文字コード | CSV は **UTF-8** |
| NFR-3 | 同時実行 | **1タイマーのみ**（FR-1.4） |
| NFR-4 | プライバシー | セッションは **本人の Firebase プロジェクト**にのみ保存。第三者アナリティクスに送らない |
| NFR-5 | 認証 | 利用前に **Google サインイン** |

## 4. ウィークリー集計の境界

- **月曜始まり**（`weekStartsOn: 1`）。

## 5. 参照

- `要求定義.md`
- `技術定義.md`
- `imput.md`
